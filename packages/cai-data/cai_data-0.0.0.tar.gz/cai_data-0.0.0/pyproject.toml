[tool.poetry]
name = "cai-data"
version = "0.0.0"
description = "A data engineering package for Causal AI."
authors = ["causaLens <opensource@causalens.com>"]
readme = "README.md"
packages = [{include = "cai_data"}]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
