query GetTasksByStatus($status: task_status_enum!) {
    task(where: {
        status: {
            _eq:$status
        },
        killed: {
            _eq: false
        }
    }) {
        id,
        flavor,
        subject,
        attempt
    }
}
