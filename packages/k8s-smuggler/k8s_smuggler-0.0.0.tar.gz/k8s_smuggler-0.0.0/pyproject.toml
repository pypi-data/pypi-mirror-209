[tool.poetry]
name = "k8s-smuggler"
version = "0.0.0"
description = ""
authors = ["DiegoPomares"]
readme = "readme.md"

[tool.poetry.scripts]
smuggler = "k8s_smuggler.main:main"

[tool.poetry.dependencies]
python = "^3.10"
click = "^8.1.3"
kubernetes = "^23"

[tool.poetry.group.dev.dependencies]
pylint = "^2.17.4"
pytest = "^7.3.1"
pytest-cov = "^4.0.0"
pytest-pylint = "^0.19.0"
pytest-xdist = "^3.3.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
