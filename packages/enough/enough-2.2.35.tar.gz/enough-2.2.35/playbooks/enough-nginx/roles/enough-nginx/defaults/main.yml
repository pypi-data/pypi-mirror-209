---
# enough_nginx_fqdn: "mydomain.com"
enough_nginx_reverse_proxy_name: '{{ enough_nginx_fqdn }}'
# enough_nginx_proxy_pass: 
enough_nginx_sites: ""
# enough_nginx_reverse_proxy:
enough_nginx_cache: false
enough_nginx_client_max_body_size: "200M"
