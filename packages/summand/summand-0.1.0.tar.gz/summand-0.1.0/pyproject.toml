[tool.poetry]
name = "summand"
version = "0.1.0"
description = ""
authors = ["Alireza <alirezaaraby5@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
summand = "summand.cli:app"

[tool.poetry.dependencies]
python = "^3.9"
typer = {extras = ["all"], version = "^0.7.0"}
tinydb = "^4.7.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
