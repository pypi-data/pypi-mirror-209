[build-system]
requires = ["setuptools>=42.0.2", "pkutils>=3.0.0,<4.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "api-utils"

[tool.isort]
profile = "black"
skip_gitignore = true
line_length = 88
multi_line_output = 3
lines_between_types = 1
combine_as_imports = true
