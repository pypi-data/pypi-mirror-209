[tool.poetry]
name = "cai-features"
version = "0.0.0"
description = "Causal AI Feature Engineering"
authors = ["causaLens <opensource@causalens.com>"]
readme = "README.md"
packages = [{include = "cai_features"}]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
