from setuptools import setup, find_packages

setup(
    name='lr_sgd',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[
    #dependencies 
    ],
    description='a simple linear regression model with sgd'

)