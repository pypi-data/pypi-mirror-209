[tool.poetry]
name = "cai-algorithmic-recourse"
version = "0.0.0"
description = "Algorithmic Recourse"
authors = ["causaLens <opensource@causalens.com>"]
readme = "README.md"
packages = [{include = "cai_algorithmic_recourse"}]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
