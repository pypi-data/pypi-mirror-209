[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "sallyjr"
version = "0.1.0"
description = "Python client for the Sally Jr API"
authors = ["Michael Daigler <michael@conncreekroad.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.25.1"

[tool.poetry.dev-dependencies]

[tool.poetry.scripts]
