[tool.poetry]
name = "cai-metrics"
version = "0.0.0"
description = "Metrics for Causal AI"
authors = ["causaLens <opensource@causalens.com>"]
readme = "README.md"
packages = [{include = "cai_metrics"}]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
