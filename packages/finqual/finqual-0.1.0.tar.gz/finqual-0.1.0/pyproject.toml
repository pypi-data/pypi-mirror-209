[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "FinQual"
version = "0.1.0"
authors = [
  { name="Myztika"},
]
description = "A WIP package to retrieve financial information"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]